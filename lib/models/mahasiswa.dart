class Mahasiswa {
  String id, name, nim, kelas;
  DateTime createdAt;

  Mahasiswa(
      {required this.nim,
      required this.id,
      required this.kelas,
      required this.name,
      required this.createdAt});
}
